raw/
people.md
mirror/
wiki/people/
# core/private/ replaces the stale wiki/placements/ entry — that path
# doesn't exist anymore post-migration, this is the one that matters now.
core/private/
journal/
.claude/settings.json
.claude/settings.local.json
.claude/transcripts/
.claude/workflows/
.codex/config.toml
*.ps1
.docs/
.obsidian

# Generated build output — never hand-edited, always regenerated by the compilers
site/dist/
site/src/data/generated/

node_modules/
__pycache__/
*.pyc
*.log
*.jsonl

# Environment secrets
.env
.env.local
.env.*

# Diagnostic hook output — not real vault content, shouldn't be tracked
scripts/diagnostic-log.jsonl
