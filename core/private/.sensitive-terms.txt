# One term per line. Anything here gets checked against every generated
# public JSON file (scripts/verify_boundaries.py) and, if the PreToolUse
# hook is wired up, against any write into an export-authorized path
# before that write is even allowed to happen.
#
# No length minimum here on purpose — the original leak check silently let
# through anything 4 characters or shorter, which would have missed a
# 3-letter company name entirely.
#
# Replace these placeholder examples with your actual terms:
# TCS
# Cognizant
# <specific compensation figures>
# <specific recruiter or offer names>
